generated: '2026-09-17'
method: generated
source: openapi/*.yml, conventions/medium-conventions.yml, errors/medium-problem-types.yml,
  data-model/medium-data-model.yml
description: >-
  Packaged Agent Skills for Medium's REST API. Every operationId referenced below was verified
  against the six refined OpenAPIs in this repository — none is invented. Both skills are written
  draft-first on purpose: the contract has no delete, no unpublish, no update and no idempotency
  key, so an agent that publishes to a real Medium profile cannot undo it.
  Medium publishes no skills or AGENTS.md of its own; these are generated by API Evangelist.
provider_published: false
skills:
- name: medium-publish-post
  file: medium-publish-post.md
  description: Publish a post to a Medium user's own profile, draft-first.
  operations: [getAuthenticatedUser, uploadImage, createUserPost]
- name: medium-publish-to-publication
  file: medium-publish-to-publication.md
  description: Publish into a publication after resolving the caller's editor/writer role.
  operations: [getAuthenticatedUser, listUserPublications, listPublicationContributors, createPublicationPost]
caveat: >-
  Medium does not accept new API integrations and has not issued new integration tokens since 2023.
  These skills are usable only by a caller who already holds a valid token.
